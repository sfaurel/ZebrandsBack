services:
  api:
    build: .
    container_name: zebrands-accounts-service
    ports:
      - "8002:8000"
    env_file:
      - .env
    networks:
      - zebrands-net


networks:
  zebrands-net:
    external: true