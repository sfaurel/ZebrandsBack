services:
  api:
    build: .
    container_name: zebrands-accounts-service
    ports:
      - "8000:8000"
    env_file:
      - .env

    networks:
      - zebrands-net


networks:
  zebrands-net:
    external: true